// lib/prisma.js
import { PrismaClient } from '@prisma/client';

// Add deep logging to see exactly what Prisma is doing.
const prisma = new PrismaClient({
  log: [
    {
      emit: 'stdout',
      level: 'query',
    },
    {
      emit: 'stdout',
      level: 'error',
    },
    {
      emit: 'stdout',
      level: 'info',
    },
    {
      emit: 'stdout',
      level: 'warn',
    },
  ],
});

export { prisma };